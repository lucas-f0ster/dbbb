var player
var player2
var stars
var bombs
var platforms
var cursors
var score = 0
var gameOver = false
var scoreText
var pointLight
var pointLight2
var keys
var spark2

// particles
var spark
var dust

// player states
var stances = ['ball', 'dash']

export default {
  player,
  player2,
  stars,
  bombs,
  platforms,
  cursors,
  score,
  gameOver,
  scoreText,
  spark,
  dust,
  stances,
  pointLight,
  pointLight2,
  keys,
  spark2
}
